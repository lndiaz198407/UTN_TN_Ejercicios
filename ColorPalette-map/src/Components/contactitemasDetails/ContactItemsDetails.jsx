/* import React from "react"


export default function ContactItemsDetails(id, img, name) {

  return (
   <div>
            <img src={img} alt={`${name} Imagen Ptrofile`} />
            <samp>{name}</samp>
            <div>{id}</div>
    </div>
    
  )
} */


